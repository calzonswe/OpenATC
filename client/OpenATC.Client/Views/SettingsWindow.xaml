<Window x:Class="OpenATC.Client.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OpenATC Settings" Height="400" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Server Address:" Margin="0,0,8,8"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="ServerAddressBox" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Server Port:" Margin="0,0,8,8"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="ServerPortBox" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Callsign:" Margin="0,0,8,8"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="CallsignBox" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="PTT Key:" Margin="0,0,8,8"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="PttKeyBox" Margin="0,0,0,8"
                 ToolTip="Press a key while this field is focused to bind it"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Joystick:" Margin="0,0,8,8"/>
        <ComboBox Grid.Row="4" Grid.Column="1" x:Name="JoystickCombo" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Audio Device:" Margin="0,0,8,8"/>
        <ComboBox Grid.Row="5" Grid.Column="1" x:Name="AudioDeviceCombo" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="6" Grid.Column="0" Text="Volume:" Margin="0,0,8,8"/>
        <Slider Grid.Row="6" Grid.Column="1" x:Name="VolumeSlider" 
                Minimum="0" Maximum="1" TickFrequency="0.1" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Height="30" Margin="0,0,8,0" Click="OnSaveClick"/>
            <Button Content="Cancel" Width="80" Height="30" Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>
