# Server dependencies (Python 3.12+)
fastapi>=0.136.0
uvicorn[standard]>=0.49.0
pydantic>=2.13.0
pydantic-settings>=2.14.0
websockets>=16.0
httpx>=0.28.0
numpy>=2.4.0           # NumPy 2.x required (breaking changes from 1.x; see numpy.org/devdocs/numpy_2_0_migration_guide.html)

# ML (install separately if needed; pinned in Dockerfile for reproducibility)
# faster-whisper>=1.2.0
# ollama>=0.6.0

# Testing
pytest>=9.0.0
pytest-asyncio>=1.4.0
